# recipes

Shell scripts that build the sandbox shapes listed in `.claude/skills/sandbox/SKILL.md`.
`./k sandbox <name>` has `fresh` and `conflict` built in; add the rest here as the days that need
them are written.
