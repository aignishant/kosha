# Pins

Tool versions this repository has been verified against. Regenerate rather than trust; Principle 11.

| Tool | Version | Verified | How to check |
|---|---|---|---|
| Git | `TODO(me)` | | `git --version` |
| `gh` | `TODO(me)` | | `gh --version` |
| `git-filter-repo` | not yet installed — Day 103 | | `git filter-repo --version` |
| Git LFS | not yet installed — Day 95 | | `git lfs version` |

Day 0's build brief asks you to fill the first two rows. Later days add theirs as the tool is first
used, not up front.
